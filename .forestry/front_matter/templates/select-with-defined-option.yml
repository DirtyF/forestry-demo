---
label: Select with defined option
hide_body: true
fields:
- name: custom-menu
  type: select
  default: ''
  config:
    required: false
    options:
    - tacos
    - wines
    - sodas
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Menu Custom
- name: list
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: simple
    options:
    - one
    - two
    - three
  label: list
